#include <iostream>
#include <map>
#include <vector>

using namespace std;

// Function to find the key values
// according to given mapped value K
void printKey(map<string, int> &Map,
              vector<int> &K, vector<string> &faultnames);